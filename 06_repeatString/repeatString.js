const repeatString = function(str, num) {
    let repword = "";
    for (let i = 0; i < num; i++) {
        repword += str;
    }
    return repword;
};

// Do not edit below this line
module.exports = repeatString;
